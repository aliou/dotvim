
local M = {}
-- Retrieve diff hunks and add them to the qflist.
M.diff = function()
  return {}
end

-- Retrieve merge conflicts and add them to the qflist.
M.merge = function()
  return {}
end

return M
